# ── Dev / CI only — ติดตั้งเพิ่มจาก requirements.txt (pinned, G14) ──────────
# pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
ruff==0.16.8              # ให้ตรงกับ rev ใน .pre-commit-config.yaml
pre-commit==4.6.2
